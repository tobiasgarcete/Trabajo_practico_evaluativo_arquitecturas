{
  "name": "supermarket-mongo-vanilla",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/api",
    "build": "npm run build -w apps/api"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}